const fs = require('fs');

// const crabs = fs.readFileSync('./input_treacheryOfWhales.txt').toString().split(',').map(Number);

const crabs = fs.readFileSync('./example_treacheryOfWhales.txt').toString().split(',').map(Number);
let leastFuel = 0;

for(let pos = Math.min(...crabs); post < Math.max(...crabs); pos){
}

console.log();
